# DO NOT commit real secrets. This is a template.
# Create the actual Secret with:
#   kubectl create secret generic stock-api-key \
#     --from-literal=ADD_API_KEY=<your-key> \
#     --dry-run=client -o yaml | kubectl apply -f -
apiVersion: v1
kind: Secret
metadata:
  name: stock-api-key
  namespace: default
type: Opaque
stringData:
  ADD_API_KEY: "REPLACE_ME"
