apiVersion: v1
kind: Service
metadata:
  name: app1-realtime-dashboard
spec:
  selector:
    app: app1-realtime-dashboard
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
